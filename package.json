{
  "name": "choosimple",
  "version": "1.0.0",
  "description": "Choosimple static site built with Eleventy.",
  "scripts": {
    "start": "eleventy --input=src --output=_site --serve",
    "build": "eleventy --input=src --output=_site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
