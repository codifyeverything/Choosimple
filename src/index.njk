---
layout: base.njk
permalink: /index.html
title: "Choosimple | Best Products. One Clear Decision."
description: "Choosimple helps you find the best product for your situation with one clear pick per category. No top-10 lists. Just the right answer and the proof behind it."
extraHead: |
  <style>
    .hero-grid.hero-grid--focused {
      align-items: start;
    }
  
    .hero-right {
      align-self: start;
    }
  
    .hero-card {
      box-sizing: border-box;
      opacity: 1;
      transition: opacity 0.35s ease;
    }
  
    .hero-card.is-fading {
      opacity: 0;
    }
  
    .featured-image-wrap {
      height: 130px;
      min-height: 130px;
      max-height: 130px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 0 0.7rem;
      overflow: hidden;
    }
  
    #featured-image {
      display: block;
      width: auto;
      max-width: 100%;
      max-height: 110px;
      height: auto;
      object-fit: contain;
      margin: 0 auto;
    }
  
    .hero-left h1 {
      font-family: "DM Sans", sans-serif;
      font-weight: 500;
      letter-spacing: -0.04em;
      line-height: 0.96;
    }
  
    .hero-left h1 em {
      font-family: "DM Sans", sans-serif;
      font-style: italic;
      font-weight: 600;
      color: #2F5A1C;
    }
  
    .catalogue-intro {
      margin-bottom: 1rem;
    }
  
    .mini-how {
      margin-top: 0;
      margin-bottom: 1.1rem;
      padding-top: 0;
      padding-bottom: 0;
    }
  
    .cat-group#kitchen {
      margin-top: 0;
    }
  
    @media (min-width: 768px) {
      .hero {
        padding-top: 0.15rem;
        padding-bottom: 0.2rem;
      }
  
      .hero-grid.hero-grid--focused {
        grid-template-columns: minmax(0, 1fr) 432px;
        gap: 50px;
        align-items: start;
        min-height: 500px;
      }
  
      .hero-left {
        padding-top: 0.95rem;
        padding-bottom: 0;
        max-width: 600px;
      }
  
      .hero-eyebrow {
        margin-bottom: 0.7rem;
      }
  
      .hero-left h1 {
        max-width: 8.8ch;
        margin-bottom: 1.05rem;
      }
  
      .hero-buttons {
        margin-bottom: 0.95rem;
      }
  
      .hero-helper {
        max-width: 34rem;
        margin-top: 0.45rem;
      }
  
      .hero-right {
        width: 432px;
      }
  
      .hero-card {
        padding: 28px 24px 24px;
        min-height: 540px;
      }
  
      .hero-card-eyebrow {
        margin-bottom: 0.7rem;
      }
  
      .hero-card h3 {
        margin-bottom: 0.35rem;
        font-size: 26px;
        line-height: 1.05;
      }
  
      .hero-card-sub {
        margin-bottom: 0.8rem;
      }
  
      .hero-card-rule {
        margin: 0.85rem 0;
      }
  
      .hero-card-quote {
        margin-bottom: 0.9rem;
        font-size: 16px;
        line-height: 1.52;
      }
  
      #featured-title {
        min-height: 82px;
      }
  
      #featured-quote {
        min-height: 80px;
      }
  
      #featured-proof {
        min-height: 88px;
      }
  
      .catalogue {
        padding-top: 0.1rem;
      }
    }
  
    @media (max-width: 767px) {
      .hero {
        padding-top: 1rem;
        padding-bottom: 0.15rem;
      }
  
      .hero-grid.hero-grid--focused {
        align-items: start;
        gap: 0.45rem;
      }
  
      .hero-right {
        align-self: start;
        margin-top: 0;
      }
  
      .hero-left h1 {
        margin-bottom: 0.75rem;
        max-width: 8.5ch;
        line-height: 0.98;
      }
  
      .hero-body {
        margin-bottom: 0.75rem;
      }
  
      .hero-buttons {
        margin-bottom: 0.1rem;
      }
  
      .hero-helper {
        display: none;
      }
  
      .catalogue {
        padding-top: 0.2rem;
      }
  
      .catalogue-intro {
        margin-bottom: 0.35rem;
      }
  
      .mini-how {
        display: none;
        margin: 0;
        padding: 0;
      }
  
      .cat-group#kitchen {
        margin-top: 0;
      }
  
      .featured-image-wrap {
        height: 100px;
        min-height: 100px;
        max-height: 100px;
        margin: 0 0 0.75rem;
      }
  
      #featured-image {
        max-height: 84px;
      }
  
      .hero-card {
        min-height: 430px;
      }

      #featured-title {
        min-height: 62px;
      }

      #featured-sub {
        min-height: 42px;
      }

      #featured-quote {
        min-height: 96px;
      }

      #featured-proof {
        min-height: 118px;
      }
    }
  </style>
---

<section class="hero">
<div class="hero-grid hero-grid--focused">
<div class="hero-left">
<p class="hero-eyebrow">BEST PRODUCT PICKS FOR REAL BUYERS - BACKED BY RELIABILITY SIGNALS AND BUYER PATTERNS</p>
<h1>Best products. One clear decision.</h1>
<div class="hero-buttons">
<a class="btn btn-primary" href="#quick-shop">Find your product</a>
</div>
<p class="hero-helper">Start with the product you are actually shopping for, then move straight to the decision and the proof behind it. For blenders, the best pick for most people is the Vitamix 5200.</p>
<p class="home-brand-proofline">Choosimple gives you one clear decision — based on real ownership patterns, not review noise or spec-sheet overload.</p>
</div>
<div class="hero-right">
<a aria-label="View featured decision details" class="hero-card hero-card--featured-link" href="/blender.html" id="hero-card">
<p class="hero-card-eyebrow">Choosimple featured decision</p>
<div class="featured-image-wrap">
<img alt="Vitamix 5200" id="featured-image" src="/Vitamix_5200.png"/>
</div>
<h3 id="featured-title">Vitamix 5200</h3>
<p class="hero-card-sub" id="featured-sub">Kitchen · Typical price: $350–$480</p>
<div class="hero-card-rule"></div>
<p class="hero-card-quote" id="featured-quote">“The best blender for most people: Vitamix 5200 — proven durability, consistent performance, and simple long-term ownership.”</p>
<p class="hero-card-proof" id="featured-proof"><strong>Why it wins:</strong> proven motor durability, consistent results across smoothies, frozen fruit, nut butters, and soups, and a simple low-failure design.</p>
<p class="hero-card-note">Want a different category? Start with the buttons on the left.</p>
</a>
</div>
</div>
</section><section class="quick-shop" id="quick-shop">
<div class="quick-shop-inner">
<div class="quick-shop-copy">
<span class="eyebrow">Start here</span>
<h2>What are you shopping for?</h2>
</div>
<div class="quick-shop-grid">
<a class="quick-shop-card" href="#kitchen">
<span class="quick-shop-label">Kitchen</span>
<strong class="quick-shop-products">Blender, air fryer, coffee grinder</strong>
<span>See kitchen picks →</span>
</a>
<a class="quick-shop-card" href="#home-cat">
<span class="quick-shop-label">Home Essentials</span>
<strong class="quick-shop-products">Robot vacuum, air purifier, video doorbell</strong>
<span>See home picks →</span>
</a>
<a class="quick-shop-card" href="#personal-care">
<span class="quick-shop-label">Personal Care</span>
<strong class="quick-shop-products">Electric toothbrush, hair straightener</strong>
<span>See personal care picks →</span>
</a>
</div>
</div>
</section><section class="micro-signup" id="email-signup">
<div class="micro-signup-inner">
<div class="micro-signup-copy">
<span class="eyebrow">Get updates</span>
<h2>Still deciding?</h2>
<p>Stay up to date with the latest picks and category launches</p>
</div>
<form action="ACTION-URL-HERE" class="micro-signup-form" method="post" target="_blank">
<input name="EMAIL" placeholder="you@example.com" type="email"/>
<button type="submit">Get updates</button>
</form>
</div>
</section><section class="catalogue" id="catalogue">
<div class="catalogue-header">
<div class="catalogue-header-left">
<span class="eyebrow">Browse by category</span>
<h2>Shop by category.<br/>Get one clear answer.</h2>
</div>
</div>
<section class="mini-how" id="how">
<div class="mini-how-grid">
<div class="mini-how-item">
<span>01</span>
<h3>Start with the decision</h3>
<p>Get the one pick that makes the most sense before spending time on edge cases.</p>
</div>
<div class="mini-how-item">
<span>02</span>
<h3>Use the proof</h3>
<p>See the buyer mistakes, misleading signals, and ownership tradeoffs that support the decision.</p>
</div>
<div class="mini-how-item">
<span>03</span>
<h3>Check the exceptions</h3>
<p>Only look at alternatives if your situation clearly calls for one.</p>
</div>
</div>
</section>
<section class="cat-group" id="kitchen">
<div class="cat-group-header">
<h3>Kitchen</h3>
<span class="cat-group-count">{{ categories | groupCount('Kitchen') }} decisions</span>
</div>
<div class="card-grid">
{% for item in categories %}{% if item.group == "Kitchen" %}
{% include "category-card.njk" %}
{% endif %}{% endfor %}
</div>
</section>
<section class="cat-group" id="home-cat">
<div class="cat-group-header">
<h3>Home Essentials</h3>
<span class="cat-group-count">{{ categories | groupCount('Home Essentials') }} decisions</span>
</div>
<div class="card-grid">
{% for item in categories %}{% if item.group == "Home Essentials" %}
{% include "category-card.njk" %}
{% endif %}{% endfor %}
</div>
</section>
<section class="cat-group" id="personal-care">
<div class="cat-group-header">
<h3>Personal Care</h3>
<span class="cat-group-count">{{ categories | groupCount('Personal Care') }} decisions</span>
</div>
<div class="card-grid">
{% for item in categories %}{% if item.group == "Personal Care" %}
{% include "category-card.njk" %}
{% endif %}{% endfor %}
</div>
</section>
</section>

<script>
(function () {
  const featuredDecisions = [
{% for item in categories %}{% if item.featured %}
    {
      title: {{ item.decision | dump | safe }},
      sub: {{ item.featuredSub | dump | safe }},
      quote: {{ item.featuredQuote | dump | safe }},
      proof: {{ item.featuredProof | dump | safe }},
      link: {{ item.url | dump | safe }},
      image: {{ item.image | dump | safe }},
      alt: {{ item.imageAlt | dump | safe }}
    },
{% endif %}{% endfor %}
  ];

  let featuredIndex = 0;

  const heroCard = document.getElementById("hero-card");
  const featuredImage = document.getElementById("featured-image");
  const featuredTitle = document.getElementById("featured-title");
  const featuredSub = document.getElementById("featured-sub");
  const featuredQuote = document.getElementById("featured-quote");
  const featuredProof = document.getElementById("featured-proof");

  if (!heroCard || !featuredImage || !featuredTitle || !featuredSub || !featuredQuote || !featuredProof || featuredDecisions.length < 2) {
    return;
  }

  function applyFeaturedItemToCard(card, item) {
    const image = card.querySelector("#featured-image");
    const title = card.querySelector("#featured-title");
    const sub = card.querySelector("#featured-sub");
    const quote = card.querySelector("#featured-quote");
    const proof = card.querySelector("#featured-proof");

    if (image) {
      image.src = item.image;
      image.alt = item.alt;
    }

    if (title) title.textContent = item.title;
    if (sub) sub.textContent = item.sub;
    if (quote) quote.textContent = item.quote;
    if (proof) proof.innerHTML = item.proof;

    card.href = item.link;
    card.setAttribute("aria-label", `View details for ${item.title}`);
  }

  function lockFeaturedCardHeight() {
    const cardWidth = heroCard.getBoundingClientRect().width;

    if (!cardWidth) {
      return;
    }

    const measuringCard = heroCard.cloneNode(true);
    measuringCard.classList.remove("is-fading");
    measuringCard.removeAttribute("id");
    measuringCard.style.position = "absolute";
    measuringCard.style.visibility = "hidden";
    measuringCard.style.pointerEvents = "none";
    measuringCard.style.left = "-9999px";
    measuringCard.style.top = "0";
    measuringCard.style.width = `${cardWidth}px`;
    measuringCard.style.minHeight = "0";
    measuringCard.style.height = "auto";

    document.body.appendChild(measuringCard);

    let tallestCardHeight = 0;

    featuredDecisions.forEach((item) => {
      applyFeaturedItemToCard(measuringCard, item);
      tallestCardHeight = Math.max(tallestCardHeight, measuringCard.scrollHeight);
    });

    measuringCard.remove();

    heroCard.style.minHeight = `${Math.ceil(tallestCardHeight)}px`;
  }

  function updateFeaturedDecision(index) {
    applyFeaturedItemToCard(heroCard, featuredDecisions[index]);
  }

  lockFeaturedCardHeight();

  window.addEventListener("resize", () => {
    heroCard.style.minHeight = "";
    lockFeaturedCardHeight();
  });

  setInterval(() => {
    heroCard.classList.add("is-fading");

    setTimeout(() => {
      featuredIndex = (featuredIndex + 1) % featuredDecisions.length;
      updateFeaturedDecision(featuredIndex);
      heroCard.classList.remove("is-fading");
    }, 350);
  }, 3000);
})();
</script>
